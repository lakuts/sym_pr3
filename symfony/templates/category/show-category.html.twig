{% extends 'base.html.twig' %}

{% block title %}Category!{% endblock %}

{% block  main_cont %}

    <br> </br>
    <h1></h1>
    <h1>{{ category.name }} ✅</h1>

    {% for child in category.child %}
        {# <a href="{{ path('showCategory', {'id': child.id}) }}">{{ child.name }}</a><br> </br> #}
        <a href="{{ path('showCategory', {'name': child.name}) }}">{{ child.name }}</a><br> </br>
    {% endfor %}

    <br> </br>

    {% for childProduct in category.childProduct %}
        <a href="{{ path('product', {'name': childProduct.name}) }}">{{ childProduct.name }}</a><br> </br>
    {% endfor %}

    {# {% for category in subcategories %} #}
    {# <a href="{{ path('showCategory', {'id': category.id}) }}">{{ category.name }}</a><br> </br> #}
    {# <a href="{{ path('showCategory', {'name': category.name}) }}">{{ category.name }}</a><br> </br> #}
    {# {% else %} #}
    {# <a1>no records found</a1> #}
    {# {% endfor %} #}


{% endblock %}

